version: "3.7"

services:
  web:
    image: rafftyl/nostr-ntfy:1.2.0
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/data

  app_proxy:
    environment:
      APP_HOST: web
      APP_PORT: 8181
      PROXY_AUTH_ADD: "false"
    depends_on:
      - web
    restart: on-failure